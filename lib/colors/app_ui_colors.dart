import 'package:flutter/material.dart';

class AppUiColors {
  static const appBarBackgroundColor = Colors.black;
  static Color backgroundColor = Colors.grey[800];
  static const iconColor = Colors.white;
  static const selectedIconColor = Colors.amber;
  static const textColor = Colors.amber;
}
